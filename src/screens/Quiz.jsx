import { View } from "react-native"
import { Text } from "react-native-paper"

function Quiz({ navigation, route }) {
  return (
    <View>
      <Text variant="bodyMedium">Quiz screen</Text>
    </View>
  )
}

export default Quiz
